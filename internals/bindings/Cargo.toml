
[package]
edition = "2021"
name = "bindings"
version = "2.0.0"

[lib]
crate-type = ["cdylib"]
path = "lib.rs"

[dependencies]
ui-build-compression = { version = "2.0.0", path = "../core" }
napi = "3.8.6"
napi-derive = "3.5.5"

[build-dependencies]
napi-build = "2"

[profile.release]
lto = true
codegen-units = 1
strip = "symbols"
